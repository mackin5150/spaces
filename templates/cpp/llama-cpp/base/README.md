# {{name}}

Generated by `spaces`.

This workspace is set up for local llama.cpp experiments.

Suggested next steps:
1. `make bootstrap`
2. `make build`
3. Put a `.gguf` model in `models/`
4. `make run`
